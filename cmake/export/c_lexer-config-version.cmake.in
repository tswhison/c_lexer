set(PACKAGE_VERSION "@MYPROJ_VERSION@")
if(NOT "${PACKAGE_FIND_VERSION}" VERSION_GREATER "@MYPROJ_VERSION@")
  set(PACKAGE_VERSION_COMPATIBLE 1)
  if("${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@MYPROJ_VERSION@")
    set(PACKAGE_VERSION_EXACT 1)
  endif()
endif()
